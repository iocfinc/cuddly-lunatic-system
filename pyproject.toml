[project]
name = "quant-researcher-desk"
version = "0.1.0"
description = "Personal quantitative research system for daily options analysis and report generation."
readme = "AGENTS.md"
requires-python = ">=3.10"
dependencies = [
    "moomoo-api>=10.4.6408",
    "notion-client>=2.3.0",
    "QuantLib",
]

[tool.uv]
package = false

[tool.quant-researcher-desk]
status = "planning-scaffold"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
